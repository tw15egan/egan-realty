const React = require('react');

// <Header />
const Header = React.createClass({
  render : function() {
    return (
      <header>
        <h1>Egan Realty</h1>
      </header>
    )
  }
});

export default Header;
